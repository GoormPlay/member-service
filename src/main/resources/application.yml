spring:
  application:
    name: member-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
feign:
  client:
    config:
      default:
        retryer: feign.Retryer.Default
server:
  port: ${SPRING_SERVER_PORT:8082}

#?db 9000, 3306